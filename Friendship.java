public record Friendship(Animal a, Animal b) {

    // Prints friends
    public void printFriends() {
        System.out.println(a.getName() + " and " + b.getName() + " are friends.");
    }

    // What is a record?

    // Do Animals a and b function like public or private class variables?

    // How can you access a and b outside of this file?

    // Are records easier to code than classes?

    // Why can't Animal be a record?

    // Can Animal a be a Reptile and Animal b be a Human?

}
